<p id="notice"><%= notice %></p>



<button style="border: 2px solid #3cd3f2; color: #000; background-color: #fff;" class="btn btn-lg btn-info btn-responsive">
        <%= link_to 'New Job +', new_job_path %></button></a>

<h1>All Jobs</h1>

<table class="table">
  <thead>
    <tr>
	<th scope="col">ID</th>
      <th scope="col">Title</th>
	  <th scope="col">Submitted</th>
	  <th scope="col">Updated</th>
	  <th scope="col">Time PH</th>
      <!-- <th>Body</th> --> 
	    <th scope="col">Customer </th>
	     <th scope="col">Assigned To (Designer)</th>
      <th scope="col"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= link_to job.id, job %></td>
		<td style="width: 300px;"><strong><%= link_to job.title, job %></strong></td>
		<td><%= time_ago_in_words(job.created_at) %></td>
		<td><%= time_ago_in_words(job.updated_at) %></td>
		<td><%=Time.use_zone('Sydney') { Time.current }.strftime("%I:%M %p") %></td>
        <!-- <td><%= job.body %></td> --> 
		 <!-- Only make call to .name if it exists. else, echo blank. -->
		  <% if job.customer %>
		  <td><%= link_to job.customer.name, job.customer %></td>
		 <% else  %>
		  <td></td>
		  <% end %>
		  
		  <% if job.designer %>
		 <td><%= link_to job.designer.name, job.designer  %></td>
		 <% else  %>
		  <td></td>
		  <% end %>
		 
		 
        <td><%= link_to 'show', job %></td>
        <td><%= link_to 'Edit', edit_job_path(job) %></td>
        <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


